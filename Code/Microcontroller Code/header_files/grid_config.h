/*
* This header file contain all the physical parameters for the grid and seed successive distance
* All distances are in mm
*/

// lenght of the grid in mm
unsigned int length = 20 * 10;

// breadth of grid in mm
unsigned int breadth = 20 * 10;

// distance between two seeds in mm
unsigned int seed_distance = 5 * 10;

// distance the bot should move to go to next row in grid across breadth
unsigned int column_seperation = 10 * 10;

// distance between the dig module and seed sowing module on the firebird
unsigned int dig_to_seed_distance = 28 * 10;

